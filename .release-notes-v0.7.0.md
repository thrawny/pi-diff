## Highlights

- **hashline_read** / **hashline_edit** — content-anchored file edits (xxhash), strict stale-anchor errors with `hashline_read` reread hints
- Read format: `LINE│HASH│content` (1-based line numbers)
- **dryRun** on `hashline_edit` and on **edit** when using `hashlineChanges`
- BOM + CRLF/LF preservation on hashline writes
- Shared `runHashlineEdit` (single file read via `rawUtf8`)
- TUI: themed `+x -y` on `hashline_edit` call; aligned padding (no duplicate path / extra leading newlines on results)
- ADR-001: hashline vs upstream Pi `edit` (`.pi/artifacts/DECISIONS.md`)
- `scripts/analyze-hashline-edits.py` for Pi session JSONL analysis

See [CHANGELOG.md](https://github.com/buddingnewinsights/pi-diff/blob/v0.7.0/CHANGELOG.md) for full details.